<h2 class="text-2xl font-bold mb-6">ユーザー編集</h2>

<%= form_with model: resource, as: resource_name, url: registration_path(resource_name), method: :put, local: true do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>  <!-- エラーメッセージの表示 -->

  <div class="mb-4">  <!-- フォームフィールドの間に余白 -->
    <%= f.label :name, "ユーザー名", class: "block text-lg" %>  <!-- メールアドレスラベル -->
    <%= f.text_field :name, class: "form-control w-full p-2 border rounded", autofocus: true, autocomplete: "name" %>  <!-- ユーザー名入力 -->
  </div>

  <div class="mb-4">  <!-- フォームフィールド -->
    <%= f.label :password, "新しいパスワード (変更しない場合は空白で)" %>  <!-- パスワードラベル -->
    <%= f.password_field :password, class: "form-control w-full p-2 border rounded", autocomplete: "new-password" %>  <!-- パスワード入力 -->
    <% if @minimum_password_length %>
      <em><%= @minimum_password_length %>文字以上必要です</em>  <!-- パスワードの最小文字数 -->
    <% end %>
  </div>

  <div class="mb-4">  <!-- パスワード確認 -->
    <%= f.label :password_confirmation, "パスワード確認" %>  <!-- パスワード確認ラベル -->
    <%= f.password_field :password_confirmation, class: "form-control w-full p-2 border rounded", autocomplete: "new-password" %>  <!-- パスワード確認入力 -->
  </div>

  <div class="mb-4">  <!-- 現在のパスワード -->
    <%= f.label :current_password, "現在のパスワード (変更を確認するために必要)" %>  <!-- 現在のパスワードラベル -->
    <%= f.password_field :current_password, class: "form-control w-full p-2 border rounded", autocomplete: "current-password" %>  <!-- 現在のパスワード入力 -->
  </div>

  <div class="actions">  <!-- フォームアクション -->
    <%= f.submit "更新", class: "btn btn-primary" %>  <!-- 更新ボタン -->
  </div>
<% end %>

<h3 class="text-xl font-bold mt-6">アカウントを退会</h3>

<div class="mb-4">  <!-- キャンセルオプション -->
  <%= link_to "退会", registration_path(resource_name), data: { turbo_confirm: "本当にアカウントを退会しますか？", turbo_method: :delete }, class: "btn btn-error" %>  <!-- アカウントキャンセルボタン -->
</div>

<%= link_to "戻る", :back, class: "text-blue-500" %>  <!-- 戻るリンク -->
