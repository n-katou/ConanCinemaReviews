<div class="container px-4 py-6">  <!-- コンテナとパディング -->
  <div class="flex justify-between items-center mb-6">  <!-- ヘッダー -->
    <h1 class="text-3xl font-bold">映画一覧</h1>  <!-- ページのタイトル -->
  </div>

  <div class="swiper-container">  <!-- Swiperの中央配置 -->
    <div class="swiper-wrapper">  <!-- Swiperのスライドラッパー -->
      <% @movies.each do |movie| %>
        <div class="swiper-slide">  <!-- スライドの幅を設定 -->
          <%= render 'pages/page', movie: movie %>  <!-- 映画カードのレンダリング -->
        </div>
      <% end %>
    </div>

    <!-- ナビゲーションボタン -->
    <div class="swiper-button-next btn btn-circle"></div>  <!-- DaisyUIのスタイル -->
    <div class="swiper-button-prev"></div>  <!-- DaisyUIのスタイル -->
  </div>

  <script src="https://unpkg.com/swiper/swiper-bundle.min.js"></script>
  <script>
    document.addEventListener('turbo:load', () => {
      const swiper = new Swiper('.swiper-container', {
        slidesPerView: 3,  // 1つずつスライド
        spaceBetween: 10,  // スライド間のスペース
        navigation: {
          nextEl: '.swiper-button-next',
          prevEl: '.swiper-button-prev',
        },
      });
    });
  </script>
</div>

<style>
  .swiper-button-next, .swiper-button-prev {
    color: blue;  /* 矢印ボタンの色を赤に変更 */
    background: white;  /* 背景色を白に変更 */
    border-radius: 50%;  /* 丸い形状にする */
    padding: 5px;  /* ボタンの余白 */
  }

  .swiper-button-next:hover, .swiper-button-prev:hover {
    color: white;  /* ホバー時の矢印の色を白に */
    background: blue;  /* ホバー時の背景色を赤に */
  }
</style>

