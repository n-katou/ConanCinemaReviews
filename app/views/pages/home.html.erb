<div class="container mx-auto px-4 py-6">  <!-- コンテナとパディング、中央寄せ -->
  <div class="text-lg mb-4">  <!-- 説明文 -->
    <h2 class="text-2xl font-semibold mt-4">最高のコナン映画を選ぼう</h2>
    <p>名探偵コナンファンのための映画レビューサイトへようこそ！<br>
    このサイトでは、投票数に基づいて映画のランキングを紹介しています。<br>
    詳細ページにて1人3票まで投票できるので、<br>
    あなたのお気に入りの映画を選んで、ランキングに反映させましょう。</p>

    <h2 class="text-2xl font-semibold mt-4">レビューを投稿しよう</h2>
    <p>映画に関するレビューも投稿できます。印象的だったシーンや、お気に入りのキャラクターについて、他のファンと意見を共有してください。レビューを読むことで、新たな視点から映画を楽しむことができます。</p>
    <p>最新映画もランキング対象なので、ネタバレは禁止でお願いいたします！</p>
    <p>現在、AmazonPrimeVideo・Huluで視聴することができるのでランキングを参考に視聴するのも良いかと思います。</p>
  </div>
  <br><br>
  <h1 class="text-3xl font-bold">映画一覧</h1>
  <div class="mt-12 overflow-hidden relative">  <!-- Swiperセクションとの間隔を大きく、overflow-hidden適用 -->
    <div class="swiper-container">  <!-- Swiperの中央配置 -->
      <div class="swiper-wrapper">  <!-- Swiperのスライドラッパー -->
        <% @movies.each do |movie| %>
          <div class="swiper-slide" style="width: 200px;">  <!-- スライドの幅を設定 -->
            <%= render 'pages/page', movie: movie %>  <!-- 映画カードのレンダリング -->
          </div>
        <% end %>
      </div>

      <!-- ナビゲーションボタン -->
      <div class="swiper-button-next btn btn-circle"></div>  <!-- DaisyUIのスタイル -->
      <div class="swiper-button-prev"></div>  <!-- DaisyUIのスタイル -->
    </div>
  </div>
</div>
<script src="https://unpkg.com/swiper/swiper-bundle.min.js"></script>
<script>
  document.addEventListener('turbo:load', () => {
    const swiper = new Swiper('.swiper-container', {
      slidesPerView: 1, // デフォルトでは1つのスライド
      spaceBetween: 10, // スライド間のスペース
      navigation: {
        nextEl: '.swiper-button-next',
        prevEl: '.swiper-button-prev',
      },
      // レスポンシブブレイクポイントの設定
      breakpoints: {
        // 768px以上の場合
        768: {
          slidesPerView: 3, // 3つのスライドを表示
          spaceBetween: 10 // スペースは同じで
        }
      }
    });
  });
</script>

<style>
  .swiper-button-next, .swiper-button-prev {
    color: blue;  /* 矢印ボタンの色を赤に変更 */
    background: white;  /* 背景色を白に変更 */
    border-radius: 50%;  /* 丸い形状にする */
    padding: 5px;
    top: 50%;  /* 常にコンテナの中央に配置 */
    transform: translateY(-50%);  /* ボタンの中心を正確に中央に合わせる */
  }

  @media (max-width: 768px) {
    .swiper-button-next, .swiper-button-prev {
      top: auto;  /* タブレットサイズではtopの設定を解除 */
      bottom: 10px;  /* コンテナの下部から10pxの位置に配置 */
      right: 10px;  /* 右側の余白を設定 */
    }
  }

  .swiper-button-next:hover, .swiper-button-prev:hover {
    color: white;  /* ホバー時の矢印の色を白に */
    background: blue;  /* ホバー時の背景色を赤に */
  }

  .container {
    max-width: 1200px;  /* 最大幅を設定 */
    margin: 0 auto;  /* 中央寄せ */
    padding: 16px;  /* パディングを設定 */
  }

  .swiper-slide {
    width: 200px;  /* デフォルトのスライド幅 */
  }

  @media (max-width: 768px) {
    .swiper-slide {
      width: 150px;  /* タブレットサイズではスライド幅を狭く */
    }
  }

  @media (max-width: 480px) {
    .swiper-slide {
      width: 100px;  /* スマートフォンサイズではさらに狭く */
    }
  }
</style>

